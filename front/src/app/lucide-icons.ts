import { LucideAngularModule } from 'lucide-angular';
import {
  ArrowLeft,
  ArrowRight,
  Calendar,
  Car,
  Eye,
  Gauge,
  Hash,
  Home,
  LoaderCircle,
  Moon,
  MoreHorizontal,
  Pencil,
  PlusCircle,
  Sun,
  Trash2,
} from 'lucide-angular/src/icons';

/** Shared Lucide icons for standalone `imports` and `importProvidersFrom`. */
export const lucideIcons = LucideAngularModule.pick({
  ArrowLeft,
  ArrowRight,
  Calendar,
  Car,
  Eye,
  Gauge,
  Hash,
  Home,
  LoaderCircle,
  Moon,
  MoreHorizontal,
  Pencil,
  PlusCircle,
  Sun,
  Trash2,
});
